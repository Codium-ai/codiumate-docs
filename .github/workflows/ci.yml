name: ci 
on:
  workflow_dispatch:

jobs:
  update-docs-context:
    if: github.ref == 'refs/heads/docs-context'
    runs-on: ubuntu-latest
    steps:
      - run: python scripts/build_docs_context.py
  